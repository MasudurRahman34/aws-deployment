services:

  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "3000:3000"

    # volumes:
    #   - .:/app
    #   - /app/node_modules
    #   - /app/.next

    # command: npm run dev

    restart: unless-stopped

    networks:
      - backend_default


networks:
  backend_default:
    external: true